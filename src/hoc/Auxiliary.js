// import React from 'react';
// As we are not using any JSX code or any react code particularly hence we dont need to import React in this case

const Auxiliary = (props) => props.children;

export default Auxiliary;
